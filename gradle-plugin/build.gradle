archivesBaseName = 'scorpio-gradle-plugin'

apply plugin: 'groovy'

dependencies {
    implementation gradleApi()
    implementation localGroovy()
}


uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: mavenLocal().getUrl())
        }
    }
}
